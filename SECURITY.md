# Security policy

Report vulnerabilities through private repository security advisories. The public project contains no credentials, customer telemetry or production device addresses. Synthetic replays cannot authorize infrastructure changes. Production collectors require workload identity, scoped targets, retained attestation, signed revisions, independent approval, canary thresholds and tested rollback.
